# -*- coding: utf-8 -*-
# vim: ft=yaml
#
# Set default values.
---
values:
  archive:
    source:
    source_hash:
  package:
    dearchive_path: /tmp/config-xdr_unroll.d
  pkg:
    name: cortex-agent
  rootgroup: root
  config_dir: '/etc/panw'
  service:
    name: traps_pmd
    unit_file: /etc/systemd/system/traps_pmd.service
...
