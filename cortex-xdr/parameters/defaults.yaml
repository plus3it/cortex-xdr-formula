# -*- coding: utf-8 -*-
# vim: ft=yaml
#
# Set default values.
---
values:
  archive:
    source:
    source_hash:
  package:
    install_path: /opt/traps
  pkg:
    name: cortex-agent
  rootgroup: root
  config: '/etc/panw'
  service:
    name: traps_pmd
    unit_file: /etc/systemd/system/traps_pmd.service
...
