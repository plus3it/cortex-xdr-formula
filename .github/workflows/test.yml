name: Run salt tests
on:
  pull_request:

concurrency:
  group: test-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

jobs:
  test:
    uses: plus3it/actions-workflows/.github/workflows/test-salt-linux.yml@6f37b40e09c5ead92ce7e168f8e8b2aee123461c
    strategy:
      matrix:
        os_version:
          - 8
          - 9
        salt_state:
          - cortex-xdr
        salt_pillar_root:
          - ./tests/pillar/test-cortex-xdr
    with:
      salt-os-version: ${{ matrix.os_version }}
      salt-state: ${{ matrix.salt_state }}
      salt-pillar-root: ${{ matrix.salt_pillar_root }}
